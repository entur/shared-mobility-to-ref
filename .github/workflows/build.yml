name: "Subworkflow: Lint and Build"

on:
  workflow_call:
    inputs:
      version:
        description: version to deploy
        type: string
        required: true
jobs:
  helm-lint:
    name: "Helm Lint"
    uses: entur/gha-helm/.github/workflows/lint.yml@v1
    strategy:
      matrix:
        environment: [dev, tst]
    with:
      environment: ${{ matrix.environment }}
  Openapi-lint:
    name: "OpenAPI Lint"
    uses: entur/gha-api/.github/workflows/lint.yml@v3
    secrets: inherit
    with:
      upload_to_bucket: false
  docker-lint:
    name: "Docker Lint"
    uses: entur/gha-docker/.github/workflows/lint.yml@v1
  gradle-build:
    name: "Build with gradle and run tests"
    runs-on: ubuntu-latest
    permissions:
      contents: write  # dependency-graph
    env: # Definerer globale miljøvariabler for jobben
      ARTIFACTORY_AUTH_USER: ${{ secrets.ARTIFACTORY_AUTH_USER }}
      ARTIFACTORY_AUTH_TOKEN: ${{ secrets.ARTIFACTORY_AUTH_TOKEN }}
      JVM_OPTS: -Xmx3200m
      # Bruker `head_ref` for PR-hendelser. Dette sikrer at vi får riktig PR-branch i stedet for en midlertidig merge-branch.
      # Faller tilbake på `ref_name` fra eventet for andre tilfeller som push til main eller manuell bygg og publish.
      BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
      GHA_BUILD_VERSION: ${{ inputs.version }}
      ENTUR_SONAR_PASSWORD: ${{ secrets.ENTUR_SONAR_PASSWORD }}
      SONAR_SCAN: ${{ inputs.runSonarScan }}
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH_NAME }} # Bruker BRANCH_NAME-variabelen definert i env
          fetch-depth: 0  # Hele historikken. Økt fetch-depth kan løse problemer med PR-spesifikke refs som trenger fullstendig historikk.
      - name: "Set up JDK 21"
        uses: actions/setup-java@v4
        with:
          java-version: 21.0.2
          distribution: temurin
      - name: "Setup Gradle"
        uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a
        with:
          dependency-graph: generate-and-submit
      - name: "Restore Gradle Cache"
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: v1-dependencies-${{ hashFiles('**/build.gradle.kts') }}
          restore-keys: |
            v1-dependencies-

      - name: "Download Gradle Dependencies"
        run: ./gradlew dependencies

      - name: "Check Kotlin code formatting with ktlintCheck"
        run: ./gradlew ktlintCheck

      - name: "Build with Gradle Wrapper"
        run: ./gradlew build -Pversion=${{ env.GHA_BUILD_VERSION }} -x test
      - name: "Run tests with detailed logging"
        run: ./gradlew test --scan

      - name: "Upload test results"
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: build/reports/tests/test

      - name: "Archive Build Artifacts"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: build/libs
          retention-days: 1
          overwrite: true
    outputs:
      version: ${{ env.GHA_BUILD_VERSION }}